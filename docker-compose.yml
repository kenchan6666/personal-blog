services:
  mongo:
    image: mongo:7
    ports:
      # Host 27017 often taken by other local Mongo instances.
      - "27019:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"

volumes:
  mongo_data:
