# 1.0.18 — 先写履历，再同步知识库

相对 1.0.17：Owner 说更新 CV 时，只有一份就改那一份，多份改 `updatedAt` 最近的一份，不先问是哪一份。回复在 `portfolio_update_resume` 和 `portfolio_generate_resume` 返回之后才说简历已更新。若模型先声称已更新再去写知识库，运行时改去列出并写入履历。

## 模型

与 1.0.17 相同。
