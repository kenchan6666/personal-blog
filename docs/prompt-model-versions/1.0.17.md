# 1.0.17 — 履历写完后把事实写入知识库

相对 1.0.16：整页简历的完成条件包含「关于我」写入已返回。`portfolio_list_knowledge` 之后，已有条目走 `portfolio_update_knowledge`，没有则 `portfolio_remember_knowledge`。模型若停在「首先，更新教育背景」这类预告，运行时改为调用 `portfolio_list_knowledge`，再次预告则要求把履历里的事实放进写入参数。

## 模型

与 1.0.16 相同。
