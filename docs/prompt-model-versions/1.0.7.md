# 1.0.7 — Public guide must finish the answer

相對 1.0.6：公開導覽輸出下限改為 4096 token。請求先嘗試關閉 Gemini thinking（`thinking_budget=0`，UniAPI 若 400 則回退）。若回 `finish_reason=length`／`max_tokens` 或句子停在連接詞／半句，自動續寫最多五次，訪客只看到連續完整回覆。

## 模型

與 1.0.6 相同。
