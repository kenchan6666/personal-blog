# 1.0.10 — Owner Agent 可显式发布

相对 1.0.9：Owner 明确说发布 / publish 时，Agent 调用 `portfolio_publish_content`。新建仍为 Draft；`update_content` 不能把 Draft 升为 published，但编辑已上线内容时保持 published，不再误下架。发布后仍须同步已确认事实到 RAG。

## 模型

与 1.0.9 相同。
