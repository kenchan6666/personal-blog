# 1.0.16 — 更新 CV 不再去读技能文件

相对 1.0.15：Owner 对话没有 `read_file`。`write-resume` 设为 `always: true`，整页步骤在每轮 Active Skills 里。`AGENTS.md` / `TOOLS.md` / `SOUL.md` 改为直接按该步骤调用履历 MCP。模型若停在「无法读取 write-resume」，运行时改为调用 `portfolio_list_resumes` 和 `portfolio_list_knowledge`。

## 模型

与 1.0.15 相同。
