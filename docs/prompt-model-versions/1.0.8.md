# 1.0.8 — Owner Agent site map

相對 1.0.7：Owner Agent 不再把首页当成名叫 `main` 的 About 页。首页 `/` 是 SiteProfile（`get_site` / `update_site`）；About `/about` 是模块列表。MCP 对 `about`+`main` 返回明确错误；若模型只预告「列出 About」，执行层会改去读首页并列出模块。

## 模型

與 1.0.7 相同。
