# 1.0.5 — Execute announced GitHub reads

相對 1.0.4：Owner 預設 4096 token，避免沒提 GitHub 時仍被 1024 截斷。若模型只寫「我將讀取 fabric_demo 的 README」卻沒發 function call，執行層把這句還原成 `get_github_file` 並繼續跑工具。

## 模型

與 1.0.4 相同。
