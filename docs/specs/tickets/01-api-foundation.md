## Parent

https://github.com/kenchan6666/personal-blog/issues/1

## What to build

Stand up the FastAPI Portfolio API with MongoDB (Beanie) and Redis connected. Expose a health check and an API integration-test harness at the HTTP seam so later tickets can assert external behaviour without poking persistence internals.

## Acceptance criteria

- [ ] API process starts with configured Mongo and Redis
- [ ] Health endpoint returns success when dependencies are up
- [ ] At least one API-level integration test runs green against the harness
- [ ] Document how to run API + tests locally for later tickets

## Blocked by

None — can start immediately.
