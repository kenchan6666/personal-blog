# Owner auth via email OTP (SMTP)

The sole Owner signs in with a one-time code emailed over SMTP (from the configured mailbox), not a password. This keeps the attack surface small for a single-operator CMS and matches “email login” without operating a full identity provider.
